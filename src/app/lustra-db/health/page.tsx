import { redirect } from 'next/navigation';

export default function HealthPage() {
// Automatically redirect to the first tab when visiting the base Health route
//   redirect('/lustra-db/health/activities');
  return null;
}
